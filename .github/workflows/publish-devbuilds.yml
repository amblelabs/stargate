name: Automated Builds

on:
  workflow_dispatch:
  push:
    paths:
      - common/**
      - fabric/**
      #        - forge/**
      - build.gradle
      - gradle.properties
      - settings.gradle
      - gradle/**
      - gradlew
      - gradlew.bat
      - versioning.gradle
      - .github/workflows/*

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      #    - name: Restore DataGen cache
      #      id: cache-restore
      #      uses: actions/cache/restore@v4
      #      with:
      #        path: src/main/generated
      #        key: datagen-${{ github.run_id }}
      #        restore-keys:
      #          datagen-

      #    - name: Run DataGen
      #      run: ./gradlew --configuration-cache runDatagen

      #    - name: Save DataGen cache
      #      id: cache-save
      #      uses: actions/cache/save@v4
      #      with:
      #        path: src/main/generated
      #        key: ${{ steps.cache-restore.outputs.cache-primary-key }}

      - name: Build
        run: ./gradlew :fabric:build

      - name: Publish artifacts
        uses: drtheodor/discord-webhook-upload-action@v0.3
        with:
          url: ${{ secrets.DEV_BUILDS }}
          username: tramp
          avatar: 'https://i.imgur.com/zCKLpZo.png'

          message_commit: '> - :sparkles: [${commitMessage}](<${commitUrl}>) by [${authorName}](<${authorUrl}>)'
          message_header: |
            <:new1:1253371736510959636><:new2:1253371805734015006> New :al_stargate: **Stargate: Sojourner** dev build `#${{ github.run_number }}`:

          file: |
            fabric/build/libs/*.jar
            !fabric/build/libs/*-sources.jar
            forge/build/libs/*.jar
            !forge/build/libs/*-sources.jar